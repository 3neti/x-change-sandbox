/*
 * AUTO-GENERATED BY x-change:publish
 *
 * Source: packages/x-change/resources/js/composables/usePayCodeExplorerClock.ts
 *
 * Do not edit this published host copy directly.
 * Changes will be overwritten by php artisan x-change:publish --scope=build --force.
 */
import { onBeforeUnmount, onMounted, ref, type Ref } from 'vue';

export function usePayCodeExplorerClock(enabled: Ref<boolean>): Ref<number> {
    const now = ref(Date.now());
    let timer: ReturnType<typeof setInterval> | null = null;

    onMounted(() => {
        if (enabled.value) {
            timer = setInterval(() => {
                now.value = Date.now();
            }, 30_000);
        }
    });

    onBeforeUnmount(() => {
        if (timer !== null) {
            clearInterval(timer);
        }
    });

    return now;
}
